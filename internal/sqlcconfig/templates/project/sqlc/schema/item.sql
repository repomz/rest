-- Replace "items" and its columns with the schema of your instance.
-- Keep every instance in a separate .sql file when possible.

-- Example:
-- CREATE TABLE items (
--     id         UUID PRIMARY KEY DEFAULT gen_random_uuid(),
--     name       TEXT NOT NULL,
--     created_at TIMESTAMPTZ NOT NULL DEFAULT now()
-- );
